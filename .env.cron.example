# Cron Job Environment Configuration
# Copy this to .env.cron and customize as needed

# Database Configuration
DATABASE_URL=postgresql://user:password@db:5432/workflows

# API Keys (required)
YOUTUBE_API_KEY=your_youtube_api_key_here
GOOGLE_ADS_API_KEY=your_google_ads_key_optional

# Cron Configuration
CRON_SCHEDULE=0 2 * * *        # Daily at 2 AM (default)
INGESTION_MODE=full            # full, test, or deep

# Application Settings
FASTAPI_ENV=production
LOG_LEVEL=INFO

# Alternative Cron Schedules (examples):
# CRON_SCHEDULE=0 */4 * * *     # Every 4 hours
# CRON_SCHEDULE=0 1 * * 0       # Weekly (Sundays at 1 AM)
# CRON_SCHEDULE=0 2,14 * * *    # Twice daily (2 AM and 2 PM)
# CRON_SCHEDULE=0 3 * * 1       # Every Monday at 3 AM

# Ingestion Mode Options:
# INGESTION_MODE=full           # Full ingestion (~20k workflows, 45-90 min)
# INGESTION_MODE=test           # Test ingestion (~400 workflows, 5-10 min)  
# INGESTION_MODE=deep           # Deep analysis mode (full + analytics)
